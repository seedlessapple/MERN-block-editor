import { useEditorState } from "editor/state/useEditorState";
import React, { useEffect } from "react";

type Props = {};

const GlobalEventControls = (props: Props) => {
  return <></>;
};

export default GlobalEventControls;
