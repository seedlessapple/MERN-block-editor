import { TBlockInput } from "editor/types/editor.type";
import React from "react";

type Props = { data: TBlockInput };

const Input = (props: Props) => {
  return <div>Input</div>;
};

export default Input;
